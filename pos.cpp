#include "../common/logger.h"
#include "../common/module.h"
#include "pos.h"

// ------------------------------------------------------------------
POS::POS(QObject * parent) : POS_THREAD(parent)
{
}

// ------------------------------------------------------------------
POS::~POS()
{
}

// ------------------------------------------------------------------
void POS::onChangedMain(bool on)
{
	emit sigSetMain(on);
}

// ------------------------------------------------------------------
